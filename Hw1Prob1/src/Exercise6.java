/**
 * Created by rkarlinsky on 3/3/15.
 * Working with storage(). Not exactly sure what to do here.
 * I used the code from the book that should hold in storage
 * the number of characters in a string that's fed in main.
 * can't get it to print so I know it works though.
 */
public class Exercise6 {

    public static int storage(String x) {
        int stat = x.length()*2;
        System.out.println(stat);
        return stat;

    }
// this is the problem here
    //public static void printS(){
      //  System.out.println(storage());
    }


